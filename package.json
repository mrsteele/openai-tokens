{
  "name": "openai-tokens",
  "version": "0.0.4",
  "description": "A service for calculating, managing, truncating openai tokens",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "gpt-3-encoder": "^1.1.4"
  }
}
